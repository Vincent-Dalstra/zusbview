{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-zig-dynamic",
      "type": "shell",
      "command": "zig build -Doptimize=Debug",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
